<project name="html.build" basedir="./" default="OPERATION.HTML.Build">
	
	<target name="OPERATION.HTML.Trash" description="Trashing CSS build directories">
		<echo>Trashing HTML build directories</echo>
		<delete dir="${build.html.dir}" includeemptydirs="true">
		    <include name="**/*.html"/>		    
		</delete>
	</target>

	<target name="OPERATION.HTML.Get" description="Get all HTML pages from the GAE instance">
		<echo>Getting all HTML files, and saving them to the /html/ directory</echo>
		<get src="${gae.home}" dest="${project.dir}/html/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.a}" dest="${project.dir}/html/a/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.b}" dest="${project.dir}/html/b/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.c}" dest="${project.dir}/html/c/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.d}" dest="${project.dir}/html/d/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.e}" dest="${project.dir}/html/e/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.f}" dest="${project.dir}/html/f/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.g}" dest="${project.dir}/html/g/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.h}" dest="${project.dir}/html/h/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.i}" dest="${project.dir}/html/i/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.j}" dest="${project.dir}/html/j/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.k}" dest="${project.dir}/html/k/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.l}" dest="${project.dir}/html/l/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.m}" dest="${project.dir}/html/m/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.n}" dest="${project.dir}/html/n/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.o}" dest="${project.dir}/html/o/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.p}" dest="${project.dir}/html/p/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.q}" dest="${project.dir}/html/q/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.r}" dest="${project.dir}/html/r/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.s}" dest="${project.dir}/html/s/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.t}" dest="${project.dir}/html/t/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.u}" dest="${project.dir}/html/u/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.v}" dest="${project.dir}/html/v/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.w}" dest="${project.dir}/html/w/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.x}" dest="${project.dir}/html/x/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.y}" dest="${project.dir}/html/y/index.html" verbose="true" httpusecaches="false"/>
		<get src="${gae.z}" dest="${project.dir}/html/z/index.html" verbose="true" httpusecaches="false"/>
	</target>
	<target name="OPERATION.HTML.Build">
		<ant target="OPERATION.HTML.Trash"/>
		<ant target="OPERATION.HTML.Get"/>
	</target>

</project>